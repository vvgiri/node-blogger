<!doctype html>
<html>
<head>
    <title>Blogger</title>
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css">
    <style>
        body         { padding-top:80px; word-wrap:break-word; }
    </style>
</head>
<body>
<div class="container">

    <div class="page-header text-center">
        <h1><span class="fa fa-user"></span> Blogs Page</h1>
    </div>

    <% if (typeof message !== 'undefined' && message.length > 0) { %>
        <div class="alert alert-danger"><%= message %></div>
    <% } %>

    <div class="row">

        <div class="well">
    

            <p>
                <strong>Blog Title</strong>:<%= user.blogTitle %><br>
                <strong>Blog Description</strong>:<%= user.blogDescription %><br>
                <strong>Created By</strong>:<%= user.email %><br>
            </p>
                
        </div>

    </div>
    <div class="container">
        <h3>Blog posts</h3>    
        <table class="table table-striped">
        <thead>
            <tr>
                <th>Title</th>
                <th>Created Date</th>
                <th>Updated Date</th>
                <th>View</th>
            </tr>
        </thead>
        <tbody>
            <% for(var i=0; i < posts.length; i++) { %>
            <tr>
                <td><%= posts[i].title %></td>
                <td><%= posts[i].createdDate %></td>
                <td><%= posts[i].updatedDate %></td>
                <td><a href="/post/view/<%=posts[i].id%>" class="btn btn-default btn-sm">View </a> </td>
            </tr>
            <% } %>
        </tbody>
      </table>
    </div>
</div>
</body>
</html>